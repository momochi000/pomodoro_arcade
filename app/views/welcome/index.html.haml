%h1 Pomodoro Arcade

%p Welcome to the Pomodoro Arcade, a place where you can reward yourself for your hard work.

.row-fluid
  #pomodoro-arcade-container
  -#= render :partial => 'activity_timers/default_timer'

- content_for :javascripts do
  :javascript
    PomodoroArcade.router = new PomodoroArcade.Router({container_id: "pomodoro-arcade-container"});
    Backbone.history.start();
