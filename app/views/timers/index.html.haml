.container-full
  .row
    .col-xs-12.col-sm-12.col-md-12.col-lg-12
      #pomodoro-arcade-container

- content_for :javascripts do
  :javascript
    PomodoroArcade.router = new PomodoroArcade.Router({container_id: "pomodoro-arcade-container", timer_collection: #{@backbone_timer_collection}, endpoint: "#{timers_url}"});
    Backbone.history.start();
