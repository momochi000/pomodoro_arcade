require 'spec_helper'

describe Charts::DayChart do
  context "#initialize" do
    context "without a date" do
      it "should store the given set of timers"
      it "should store the date as todays date"
      it "should set today to true"
    end
    context "with a date" do
      it "should store date"
    end
  end
end
